# 🔍 Диагностика проблемы развертки

## Проверьте следующие моменты:

### 1. ✅ Файл index.html в корне репозитория?

Откройте: https://github.com/angryrebelll-web/Calculyator

**Должен быть файл `index.html` в корне** (не в подпапке!)

Если его нет - создайте (см. инструкции ниже).

---

### 2. ✅ Файл .nojekyll в корне?

**Должен быть файл `.nojekyll`** (пустой, с точкой в начале)

Если его нет - создайте:
1. "Add file" → "Create new file"
2. Название: `.nojekyll`
3. Оставьте пустым
4. Commit

---

### 3. ✅ Настройки GitHub Pages?

Откройте: https://github.com/angryrebelll-web/Calculyator/settings/pages

**Должно быть:**
- Source: Deploy from a branch
- Branch: `main`
- Folder: `/ (root)` - **КОРЕНЬ!**

---

### 4. ✅ Проверьте Actions (workflow)

Откройте: https://github.com/angryrebelll-web/Calculyator/actions

**Что проверить:**
- Есть ли активные workflows?
- Какой статус (зеленый/красный/желтый)?
- Если красный - откройте и посмотрите ошибку

---

### 5. ✅ Структура репозитория

Должна быть такая структура:

```
Calculyator/                          ← КОРЕНЬ
├── index.html                        ← ДОЛЖЕН БЫТЬ!
├── .nojekyll                         ← ДОЛЖЕН БЫТЬ!
└── propellini-calculator-fullscreen/
    ├── index.html
    ├── calculator-fullscreen.css
    └── ...
```

---

## 🚀 БЫСТРОЕ РЕШЕНИЕ:

### Если файлов нет в корне:

1. **Создайте `.nojekyll`** (пустой файл в корне)
2. **Создайте `index.html`** (из файла на рабочем столе или из папки)
3. **Проверьте настройки** GitHub Pages
4. **Подождите 3-5 минут**

### Если файлы есть, но не работает:

1. Откройте Actions и посмотрите ошибки
2. Попробуйте изменить настройки GitHub Pages (сохраните заново)
3. Создайте новый коммит (любое изменение) - это перезапустит развертку

---

## 📋 Чек-лист:

- [ ] Файл `index.html` в корне репозитория
- [ ] Файл `.nojekyll` в корне репозитория  
- [ ] В настройках GitHub Pages выбрано `/ (root)`
- [ ] Workflow завершился успешно (зеленая галочка)
- [ ] Прошло минимум 3 минуты после последнего коммита

---

## ⚠️ Если ничего не помогает:

**Переместите все файлы в корень репозитория:**
1. Переместите все из `propellini-calculator-fullscreen/` в корень
2. В настройках выберите `/ (root)`
3. Это точно должно работать!

